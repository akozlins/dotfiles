R /

! **/lost+found
! /dev
! /proc
! /run
! /sys
! /tmp

! **/*~
! **/*.cache
! **/.local
- **/core.[0-9]*

! **/build
! **/cmake-build*
! **/meson-build*
! **/node_modules
! **/.gradle

- /boot/*.img
+ /boot
+ /etc
! /home/*/.m2
+ /home
+ /opt
+ /root
! /var/lib/docker
+ /var/lib
+ /var/log

- /*
