R /

! **/lost+found
! /dev
! /proc
! /run
! /sys
! /tmp

! **/*~
! **/*.cache
! **/.local
- **/core.[0-9]*

! **/build
! **/cmake-build*
! **/meson-build*
! **/quartus-build*
! **/node_modules
! **/.gradle
! **/.npm

- /boot/*.img
+ /boot
! /etc/ca-certificates/extracted
+ /etc
! /home/*/.m2
+ /home
+ /opt
+ /root
! /var/lib/pacman
! /var/lib/docker
+ /var/lib
+ /var/log

- /*
