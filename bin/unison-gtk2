#!/bin/bash
set -euf

export UNISON="$HOME/.config/unison"

CMD=(
    /usr/bin/unison-gtk2
    -logfile "$HOME/.cache/unison.log"
    "-watch=false"
    "$@"
)

[ $# -gt 0 ] && exec "${CMD[@]}"
exec "${CMD[@]}" &> /dev/null &
