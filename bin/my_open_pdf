#!/bin/bash
set -euf

COMMANDS=(
    evince
    zathura
    xreader
)

for cmd in "${COMMANDS[@]}" ; do
    if command -v "$cmd" &> /dev/null ; then
        exec \
        "$cmd" "$@"
        break
    fi
done
