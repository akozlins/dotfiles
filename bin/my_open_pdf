#!/bin/bash
set -euf

COMMANDS=(
    zathura
    xreader
    evince
)

for cmd in "${COMMANDS[@]}" ; do
    if command -v "$cmd" &> /dev/null ; then
        exec \
        "$cmd" "$@"
        break
    fi
done
