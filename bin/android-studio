#!/bin/bash
set -euf

export STUDIO_PROPERTIES="$XDG_CONFIG_HOME/JetBrains/AndroidStudio/properties"

CMD=(
    /usr/bin/android-studio
    "$@"
)

[ $# -gt 0 ] && exec "${CMD[@]}"
exec "${CMD[@]}" &> /dev/null &
