#!/bin/sh
set -euf -o pipefail

profile="$XDG_CONFIG_HOME/firefox/akozlins.default"

if [ ! -f "$profile/prefs.js" ] ; then
    echo "Profile '$profile' not found!"
    exit 1
fi

CMD="/usr/bin/firefox -profile $profile"

[[ -x /usr/bin/firejail ]] && CMD="/usr/bin/firejail -- $CMD"
CMD="exec $CMD $@"

if [[ $# -eq 0 ]] ; then
  $CMD &> /dev/null &
else
  $CMD
fi
