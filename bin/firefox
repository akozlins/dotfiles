#!/bin/bash
set -euf

export HOME="$DOTFILES"

CMD=(
    "/bin/firefox"
    --profile "$XDG_DATA_HOME/firefox/default"
    "$@"
)

if [ $# -eq 0 ] ; then
    exec "${CMD[@]}" > /dev/null 2>&1 &
else
    exec "${CMD[@]}"
fi
