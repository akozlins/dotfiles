#!/bin/bash
set -euf

export HOME="$DOTFILES"

CMD=(
    "/bin/firefox"
    "$@"
    --profile "$XDG_CACHE_HOME/firefox/default"
)

if [ $# -eq 0 ] ; then
    "${CMD[@]}" > /dev/null 2>&1 &
else
    "${CMD[@]}"
fi
