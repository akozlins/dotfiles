#!/bin/bash
set -euf

DOTFILES="${DOTFILES:-~/.dotfiles}"
test -d "$DOTFILES" && export HOME="$DOTFILES"

if [ -x /bin/eltclsh ] ; then
    exec \
    /bin/eltclsh "$@"
else
    exec \
    /bin/tclsh "$@"
fi
