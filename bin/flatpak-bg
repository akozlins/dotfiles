#!/bin/sh
set -euf

export HOME="$DOTFILES"

MY_LOG="$XDG_CACHE_HOME/logs/flatpak/$(echo "$*" | tr ' ' _).log"
mkdir -p -- "$(dirname "$MY_LOG")"

exec \
unbuffer \
/usr/bin/flatpak "$@" \
| ts '%Y-%m-%dT%H:%M:%S' \
>> "$MY_LOG" &
