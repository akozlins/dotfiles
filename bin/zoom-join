#!/bin/sh
set -euf

confno=$1
uname=$ZOOM_UNAME
uid=$ZOOM_UID

ZOOMMTG="zoommtg://zoom.us/join?action=join&confno=$confno&uname=$uname"

if [ $# -eq 1 ] ; then
    exec \
    zoom "$ZOOMMTG&uid=$uid"
else
    pwd=$2
    exec \
    zoom "$ZOOMMTG&pwd=$pwd&uid=$uid"
fi
