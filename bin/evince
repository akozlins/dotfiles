#!/bin/sh
set -euf

CMD=/usr/bin/evince

[ -x /usr/bin/firejail ] && CMD="/usr/bin/firejail -- $CMD"

if [ $# -eq 1 ] && [ -f "$1" ] ; then
  exec $CMD "$@" > /dev/null 2>&1 &
else
  exec $CMD "$@"
fi
