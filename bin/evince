#!/bin/sh
set -euf -o pipefail

CMD=/usr/bin/evince

[[ -x /usr/bin/firejail ]] && CMD="/usr/bin/firejail -- $CMD"
CMD="exec $CMD $@"

if [[ $# -eq 1 ]] && [[ -f "$1" ]] ; then
  $CMD &> /dev/null &
else
  $CMD
fi
