#!/bin/sh
set -euf

far2l="$HOME/.local/bin/far2l"

if [ ! -x "$far2l" ] ; then
    echo "ERROR: '$far2l' not found"
    exit 1
fi

mkdir -p -- "$XDG_CACHE_HOME/far2l/history"

exec \
"$far2l" "$@" \
> /dev/null 2>&1 &
