#!/bin/bash
set -euf

CMD=("/bin/ghb" "$@")

FJ="/bin/firejail"
[ -x "$FJ" ] && CMD=("$FJ" -- "${CMD[@]}")

if [ $# -eq 0 ] ; then
    exec "${CMD[@]}" > /dev/null 2>&1 &
else
    exec "${CMD[@]}"
fi
