#!/bin/bash
set -euf

COMMANDS=(
    celluloid
    mpv
    vlc
)

for cmd in "${COMMANDS[@]}" ; do
    if command -v "$cmd" &> /dev/null ; then
        exec \
        "$cmd" "$@"
        break
    fi
done
