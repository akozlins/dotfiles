#!/bin/bash
set -euf

COMMANDS=(
    geeqie
    nomacs
    vimiv
)

for cmd in "${COMMANDS[@]}" ; do
    if command -v "$cmd" &> /dev/null ; then
        exec \
        "$cmd" "$@"
        break
    fi
done
