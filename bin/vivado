#!/bin/sh
set -euf

HOME="$DOTFILES"

VIVADO_CACHE="$HOME/.cache/vivado"

if [ $# -eq 1 ] ; then
    mkdir -p "$VIVADO_CACHE"
    unset CDPATH
    cd "$VIVADO_CACHE" || exit 1
fi

exec \
"$XILINX_VIVADO/bin/vivado" "$@"
