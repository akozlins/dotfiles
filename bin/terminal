#!/bin/bash
set -euf

COMMANDS=(
    uxterm
    alacritty
    urxvt
)

for cmd in "${COMMANDS[@]}" ; do
    if command -v "$cmd" &> /dev/null ; then
        exec \
        "$cmd" "$@"
        break
    fi
done
