#!/bin/bash
set -euf

export HOME="$DOTFILES"

CMD=(
    "/bin/opera"
    -nomail -nomaillinks -nolirc
    "$@"
)

if [ $# -eq 0 ] ; then
    exec "${CMD[@]}" > /dev/null 2>&1 &
else
    exec "${CMD[@]}"
fi
