#!/bin/sh
set -euf -o pipefail

set -x

CMD="/usr/bin/opera -nomail -nomaillinks -nolirc"

[[ -x /usr/bin/firejail ]] && CMD="/usr/bin/firejail -- $CMD"
CMD="exec $CMD $@"

if [[ $# -eq 0 ]] ; then
  $CMD &> /dev/null &
else
  $CMD
fi
